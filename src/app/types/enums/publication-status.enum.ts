export enum PublicationStatus {
    ACTIVE = 'STATE.ACTIVE', //vigente
    FINISHED = 'STATE.FINISHED', //finalizada
    CANCELED = 'STATE.CANCELED', //cancelada, por generadora
    REJECTED = 'STATE.REJECTED', //rechazada, por transportadora
}
